import { Inter, Roboto_Mono, Sevillana } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const roboto_mono = Roboto_Mono({
  subsets: ["latin"],
  display: "swap",
});

export const sevillana_mono = Sevillana({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
